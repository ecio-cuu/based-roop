--extra-index-url https://download.pytorch.org/whl/cu118

# Núcleo científico
numpy==2.0.2
opencv-python==4.12.0.88
pillow==9.5.0
protobuf==4.25.3
tqdm==4.65.0
psutil==5.9.5

# ONNX / InsightFace
onnx==1.19.0
onnxruntime-gpu==1.20.1        ; sys_platform != 'darwin'
onnxruntime==1.20.1            ; sys_platform == 'darwin' and platform_machine != 'arm64'
onnxruntime-silicon==1.20.1    ; sys_platform == 'darwin' and platform_machine == 'arm64'
insightface==0.7.3
gfpgan==1.3.8
opennsfw2==0.10.2

# PyTorch (CUDA 11.8 en Linux/Colab)
torch==2.4.1+cu118             ; sys_platform != 'darwin'
torchvision==0.19.1+cu118      ; sys_platform != 'darwin'

# PyTorch en macOS (CPU)
torch==2.4.1                   ; sys_platform == 'darwin'
torchvision==0.19.1            ; sys_platform == 'darwin'

# UI opcional
tk==0.1.0
customtkinter==5.1.3

# TensorFlow (opcional; si lo necesitas, fija una versión compatible con NumPy 2.x)
tensorflow==2.19.1             ; sys_platform != 'darwin'

--extra-index-url https://download.pytorch.org/whl/cu118

# Núcleo científico
numpy==2.1.3
opencv-python==4.12.0.88
pillow==9.5.0
protobuf==4.23.2
tqdm==4.65.0
psutil==5.9.5

# ONNX / InsightFace
onnx==1.14.0
onnxruntime-gpu==1.20.1  ; sys_platform != 'darwin'
# (Si alguna vez lo usas en macOS Intel:)
onnxruntime==1.20.1      ; sys_platform == 'darwin' and platform_machine != 'arm64'
# (Si alguna vez lo usas en macOS Apple Silicon:)
onnxruntime-silicon==1.16.1 ; sys_platform == 'darwin' and platform_machine == 'arm64'
insightface==0.7.3
gfpgan==1.3.8
opennsfw2==0.10.2

# PyTorch (CUDA 11.8)
torch==2.2.1+cu118       ; sys_platform != 'darwin'
torchvision==0.17.1+cu118; sys_platform != 'darwin'
# PyTorch en macOS (CPU)
torch==2.2.1             ; sys_platform == 'darwin'
torchvision==0.17.1      ; sys_platform == 'darwin'

# UI opcional
tk==0.1.0
customtkinter==5.1.3

# (Opcional, solo si realmente lo necesitas)
tensorflow==2.18.0       ; sys_platform != 'darwin'

%%writefile requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu118

# Numpy + OpenCV acordes a Colab actual
numpy>=2.0.0,<2.3.0
opencv-python==4.12.0.88

# Core del proyecto
onnx==1.14.0
insightface==0.7.3
psutil==5.9.5
tk==0.1.0
customtkinter==5.1.3
pillow==9.5.0

# PyTorch (wheels cu118 disponibles en el extra-index)
torch==2.4.1+cu118; sys_platform != 'darwin'
torchvision==0.19.1+cu118; sys_platform != 'darwin'

# ONNX Runtime GPU (versiones vigentes)
onnxruntime-gpu==1.20.1; sys_platform != 'darwin'

# Otross
tensorflow==2.18.0; sys_platform != 'darwin'
opennsfw2==0.10.2
protobuf==4.23.2
tqdm==4.65.0
gfpgan==1.3.8
